# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# The commented stuff below was used in an old project of mine. Dunno if its still needed

# Set the default behavior, in case people have core.autocrlf enabled.
#* -text
#
## Explicitly declare text files you never want to be normalized and converted
## to native line endings on checkout.
#*.c text
#*.cpp text
#*.h text
#*.gd text
#*.tscn text
#*.tres text
#*.import text
#*.godot text
#*.md text
#*.py text
#*.sh text
#
## Declare files that will always have CRLF line endings on checkout.
#*.sln text eol=crlf

# Git LFS hooks, add large file types to track here.

# Audio
*.ogg filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text
*.mp3 filter=lfs diff=lfs merge=lfs -text

# Images
*.png filter=lfs diff=lfs merge=lfs -text
*.jpeg filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text
*.bmp filter=lfs diff=lfs merge=lfs -text

# Fonts
*.ttf filter=lfs diff=lfs merge=lfs -text

# Videos
*.mp4 filter=lfs diff=lfs merge=lfs -text

# Project files
*.aseprite filter=lfs diff=lfs merge=lfs -text